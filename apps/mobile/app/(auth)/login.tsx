import { Text, View } from 'react-native';

// Placeholder — rebuilt in Phase 6 (Auth): Formik + loginSchema + auth.loginUser.
export default function Login() {
  return (
    <View className="flex-1 items-center justify-center bg-bg-base px-6">
      <Text className="text-2xl font-bold text-text-primary">HMI</Text>
      <Text className="mt-2 text-text-secondary">Login — coming in Phase 6</Text>
    </View>
  );
}
