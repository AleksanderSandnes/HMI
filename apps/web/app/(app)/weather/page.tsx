// Weather station (current tiles + 6-metric Recharts charts) lands in Phase 5.
export default function WeatherPage() {
  return (
    <section>
      <h1 className="text-2xl font-extrabold text-text-primary">Weather</h1>
      <p className="mt-2 text-sm text-text-muted">Weather charts — Phase 5.</p>
    </section>
  );
}
