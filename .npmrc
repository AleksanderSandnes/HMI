# pnpm settings (node-linker, peers, overrides, only-built-dependencies) now live
# in pnpm-workspace.yaml — pnpm v10+ no longer reads them from here.
# Keep this file for registry auth / npm-only tooling settings if ever needed.
